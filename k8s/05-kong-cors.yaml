apiVersion: configuration.konghq.com/v1
kind: KongPlugin
metadata:
  name: cors-plugin
  namespace: rac-app
  annotations:
    kubernetes.io/ingress.class: kong
plugin: cors
config:
  origins:
    - http://localhost:3000
    - http://9.223.16.228
  credentials: true
  methods:
    - GET
    - POST
    - PUT
    - DELETE
    - OPTIONS
